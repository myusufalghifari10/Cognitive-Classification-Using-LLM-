# ── Default: abaikan semua, allow-list di bawah ──
*

# ── Code & config (root) ──
!*.py
!.gitignore
!README.md
!requirements*.txt

# ── Direktori konten ──
# PENTING: !dir/ cuma membuka folder-nya; file di dalamnya tetap kena rule '*'
# di atas. Harus pakai !dir/** untuk membuka isinya juga.
!/data/
!/data/**
!/prompts/
!/prompts/**
!/results/
!/results/**

# pod/ = skrip deploy RunPod (lokal, tidak dipush)

# ── Catatan internal (jangan dipush) ──
NOTE.md

# ── Backup / file antara (jangan dipush) ──
*.bak
*.premerge

# ── Python artifacts ──
__pycache__/
*.pyc
*.pyo

# ── OS / editor junk ──
.DS_Store
Thumbs.db

# ── Filter hasil benchmark: simpan laporan, abaikan aset besar ──
/results/**/*.png
/results/**/*.jsonl
/results/**/_ckpt_*
/results/**/_progress_*
!/results/**/*report*.txt
!/results/**/*report*.md

Report.pdf
